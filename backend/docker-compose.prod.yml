services:
  backend:
    build: .
    env_file: .env
    ports:
      - "7860:7860"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:7860/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
