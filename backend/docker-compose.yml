services:
  backend:
    build: .
    env_file: .env
    ports:
      - "127.0.0.1:7860:7860"
    volumes:
      - .:/app
    command: uvicorn rta_backend.main:app --host 127.0.0.1 --port 7860 --reload
